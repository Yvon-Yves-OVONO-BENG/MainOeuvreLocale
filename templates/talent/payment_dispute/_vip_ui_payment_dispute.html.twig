

	<style>
		.pd-shell{
			min-height: calc(100vh - 90px);
			background:
				radial-gradient(1100px circle at 10% 8%, rgba(220,53,69,.10), transparent 42%),
				radial-gradient(1000px circle at 92% 8%, rgba(13,110,253,.10), transparent 36%),
				radial-gradient(900px circle at 50% 100%, rgba(32,201,151,.08), transparent 40%),
				linear-gradient(180deg, #f8fafc, #ffffff, #f8fafc);
		}

		.pd-hero{
			border: 1px solid rgba(0,0,0,.06);
			border-radius: 24px;
			background: linear-gradient(180deg, rgba(255,255,255,.96), rgba(255,255,255,.84));
			box-shadow: 0 24px 70px rgba(15,23,42,.08);
			position: relative;
			overflow: hidden;
		}
		.pd-hero::before{
			content:"";
			position:absolute;
			inset:0;
			background: radial-gradient(700px circle at top right, rgba(220,53,69,.10), transparent 44%);
			pointer-events:none;
		}

		.pd-kpi{
			border-radius: 20px;
			border: 1px solid rgba(0,0,0,.06);
			background: rgba(255,255,255,.86);
			box-shadow: 0 18px 50px rgba(15,23,42,.06);
			backdrop-filter: blur(10px);
		}

		.pd-card{
			border-radius: 22px;
			border: 1px solid rgba(0,0,0,.06);
			background: linear-gradient(180deg, rgba(255,255,255,.96), rgba(255,255,255,.86));
			box-shadow: 0 22px 60px rgba(15,23,42,.07);
			overflow: hidden;
		}

		.pd-row{
			padding: 18px 20px;
			border-bottom: 1px solid rgba(0,0,0,.06);
		}
		.pd-row:last-child{
			border-bottom: 0;
		}

		.pd-soft{
			color: rgba(15,23,42,.62);
		}

		.pd-pill{
			display: inline-flex;
			align-items: center;
			gap: 8px;
			padding: .55rem .9rem;
			border-radius: 999px;
			font-weight: 800;
			border: 1px solid transparent;
		}

		.pd-open{ background: rgba(220,53,69,.12); color: #b42318; border-color: rgba(220,53,69,.20); }
		.pd-review{ background: rgba(255,193,7,.16); color: #8a6d03; border-color: rgba(255,193,7,.24); }
		.pd-resolved{ background: rgba(25,135,84,.12); color: #146c43; border-color: rgba(25,135,84,.18); }
		.pd-rejected{ background: rgba(33,37,41,.10); color: #212529; border-color: rgba(33,37,41,.14); }
		.pd-refunded{ background: rgba(13,110,253,.12); color: #0a58ca; border-color: rgba(13,110,253,.18); }

		.pd-empty{
			border-radius: 20px;
			border: 1px dashed rgba(0,0,0,.12);
			background: rgba(255,255,255,.80);
			padding: 32px 24px;
			text-align: center;
		}

		.pd-icon{
			width: 48px;
			height: 48px;
			border-radius: 16px;
			display: grid;
			place-items: center;
			font-size: 22px;
			background: rgba(220,53,69,.10);
			border: 1px solid rgba(220,53,69,.16);
		}
	</style>