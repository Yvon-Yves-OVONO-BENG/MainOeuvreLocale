<style>
  .ct-page{
    background:
      radial-gradient(1100px circle at 10% 0%, rgba(59,130,246,.10), transparent 55%),
      radial-gradient(900px circle at 90% 10%, rgba(16,185,129,.08), transparent 55%),
      linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
    min-height: 100vh;
  }
  .ct-card{
    background: rgba(255,255,255,.92);
    border: 1px solid rgba(15,23,42,.08);
    border-radius: 22px;
    box-shadow: 0 14px 40px rgba(2,6,23,.06);
  }
  .ct-soft{
    background: rgba(248,250,252,.86);
    border: 1px solid rgba(15,23,42,.07);
    border-radius: 18px;
  }
  .ct-scroll::-webkit-scrollbar{ width: 8px; height: 8px; }
  .ct-scroll::-webkit-scrollbar-thumb{ background: rgba(148,163,184,.45); border-radius: 999px; }
</style>