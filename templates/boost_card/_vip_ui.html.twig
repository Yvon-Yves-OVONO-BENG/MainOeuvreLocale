<style>
  :root{
    --mol-bg:#f6f8fc;
    --mol-card:rgba(255,255,255,.95);
    --mol-border:rgba(15,23,42,.08);
    --mol-muted:rgba(15,23,42,.62);
    --mol-text:rgba(15,23,42,.92);
    --mol-shadow:0 18px 45px rgba(2,6,23,.08);
    --mol-shadow-sm:0 10px 25px rgba(2,6,23,.06);
    --r:22px;
  }

  .pay-page{
    background:
      radial-gradient(900px circle at 10% 10%, rgba(13,110,253,.12), transparent 55%),
      radial-gradient(900px circle at 90% 10%, rgba(32,201,151,.10), transparent 55%),
      var(--mol-bg);
    min-height: 60vh;
  }

  .checkout-wrap{ max-width: 980px; margin: 0 auto; }

  .checkout-hero{
    border-radius: var(--r);
    padding: 18px 18px;
    background:
      radial-gradient(1100px circle at 12% 18%, rgba(13,110,253,.18), transparent 58%),
      radial-gradient(900px circle at 88% 12%, rgba(32,201,151,.14), transparent 55%),
      linear-gradient(180deg, rgba(255,255,255,.92), rgba(255,255,255,.72));
    border: 1px solid var(--mol-border);
    box-shadow: var(--mol-shadow-sm);
  }

  .checkout-card{
    background: var(--mol-card);
    border: 1px solid var(--mol-border);
    border-radius: var(--r);
    box-shadow: var(--mol-shadow-sm);
    overflow: hidden;
  }

  .checkout-title{
    font-weight: 950;
    letter-spacing: -.02em;
    color: var(--mol-text);
    line-height: 1.1;
  }
  .checkout-soft{ color: var(--mol-muted); }

  .secure-pill{
    display:inline-flex; align-items:center; gap:.45rem;
    padding:.42rem .7rem;
    border-radius: 999px;
    border: 1px solid rgba(13,110,253,.18);
    background: rgba(13,110,253,.08);
    color: rgba(15,23,42,.80);
    font-weight: 900;
    font-size: .85rem;
  }
  .secure-pill i{ color:#0d6efd; }

  .pay-panel{
    padding: 18px;
  }
  @media (min-width:992px){
    .pay-panel{ padding: 22px; }
  }

  .stripe-field{
    border: 1px solid rgba(15,23,42,.10);
    border-radius: 16px;
    background: rgba(255,255,255,.95);
    padding: 12px 12px;
    box-shadow: inset 0 1px 0 rgba(255,255,255,.7);
    transition: border-color .15s ease, box-shadow .15s ease, transform .15s ease;
  }
  .stripe-field:focus-within{
    border-color: rgba(13,110,253,.35);
    box-shadow: 0 0 0 4px rgba(13,110,253,.12);
    transform: translateY(-1px);
  }

  .field-label{
    font-weight: 900;
    color: rgba(15,23,42,.82);
    margin-bottom: 8px;
  }

  .cardbrand{
    font-weight: 950;
    letter-spacing: .02em;
  }

  .pay-btn{
    border-radius: 16px;
    font-weight: 950;
    padding: .8rem 1rem;
    box-shadow: 0 12px 25px rgba(13,110,253,.18);
  }

  .summary{
    height: 100%;
    border-left: 1px solid var(--mol-border);
    background: rgba(255,255,255,.55);
  }
  @media(max-width:991.98px){
    .summary{ border-left: 0; border-top: 1px solid var(--mol-border); }
  }
  .summary-inner{ padding: 18px; }
  @media (min-width:992px){ .summary-inner{ padding: 22px; } }

  .sum-box{
    border: 1px dashed rgba(15,23,42,.18);
    background: rgba(255,255,255,.85);
    border-radius: 18px;
    padding: 14px;
  }

  .sum-row{
    display:flex; justify-content:space-between; gap:10px;
    padding: 8px 0;
    border-bottom: 1px dashed rgba(15,23,42,.12);
    color: rgba(15,23,42,.80);
    font-weight: 800;
  }
  .sum-row:last-child{ border-bottom:0; }
  .sum-total{
    font-weight: 950;
    font-size: 18px;
    color: var(--mol-text);
  }

  .mini-note{
    margin-top: 12px;
    color: var(--mol-muted);
    font-weight: 700;
    font-size: .88rem;
  }

  .top-actions .btn{ border-radius: 999px; font-weight: 900; }

  #card-element{ padding: 4px 2px; }

</style>