

<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap');

  .rep-page{
    font-family: "Plus Jakarta Sans","Inter",system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;
  }

  .rep-shell{
    max-width: 1380px;
  }

  .rep-hero{
  position: relative;
  overflow: hidden;
  border-radius: 28px;
  min-height: 138px;
  border: 1px solid rgba(255,255,255,.38);
  background-image:
    linear-gradient(120deg, rgba(15,23,42,.78), rgba(15,23,42,.50)),
    radial-gradient(circle at 12% 18%, rgba(99,102,241,.20), transparent 30%),
    radial-gradient(circle at 88% 12%, rgba(16,185,129,.16), transparent 28%),
    url("https://images.unsplash.com/photo-1522202176988-66273c2fd55f?auto=format&fit=crop&w=1600&q=80");
  background-size: cover;
  background-position: center;
  box-shadow: 0 18px 50px rgba(15,23,42,.14);
  }

  .rep-hero::before{
    content:"";
    position:absolute;
    inset:0;
    background: linear-gradient(180deg, rgba(255,255,255,.02), rgba(255,255,255,.02));
    pointer-events:none;
  }

  .rep-hero-content{
    position: relative;
    z-index: 2;
  }

  .rep-hero-grid{
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(320px, .75fr);
    gap: 14px;
    align-items: end;
  }

  .rep-hero-kicker{
    display:inline-flex;
    align-items:center;
    gap:.45rem;
    padding:.38rem .72rem;
    border-radius:999px;
    background:rgba(255,255,255,.10);
    border:1px solid rgba(255,255,255,.16);
    backdrop-filter: blur(10px);
    color:rgba(255,255,255,.88);
    font-size:10.5px;
    font-weight:800;
    letter-spacing:.16em;
    text-transform:uppercase;
  }

  .rep-hero-title{
    margin-top:.7rem;
    margin-bottom:0;
    font-size:28px;
    line-height:1.02;
    font-weight:900;
    letter-spacing:-.045em;
    color:#fff;
  }

  .rep-hero-text{
    margin-top:.45rem;
    max-width:700px;
    color:rgba(255,255,255,.72);
    font-size:12.5px;
    line-height:1.55;
    font-weight:500;
  }

  .rep-hero-metas{
    margin-top:.8rem;
    display:flex;
    flex-wrap:wrap;
    gap:8px;
  }

  .rep-hero-pill{
    display:inline-flex;
    align-items:center;
    gap:.45rem;
    padding:.44rem .72rem;
    border-radius:999px;
    background:rgba(255,255,255,.12);
    border:1px solid rgba(255,255,255,.14);
    backdrop-filter: blur(10px);
    color:#fff;
    font-size:11px;
    font-weight:800;
  }

  .rep-search{
    border-radius: 20px;
    border: 1px solid rgba(255,255,255,.16);
    background: rgba(255,255,255,.12);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    box-shadow: 0 12px 32px rgba(2,6,23,.12);
    padding: 10px;
  }

  .rep-search input{
    height: 44px !important;
    font-size: 12.5px !important;
    font-weight: 600;
    border-radius: 14px !important;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
  }

  .rep-search .rep-search-btn{
    height: 36px;
    border-radius: 12px;
    font-size: 11px;
    font-weight: 800;
  }

  .rep-tabs-row{
    margin-top: 12px;
    display:flex;
    flex-wrap:wrap;
    gap:8px;
  }

  .rep-tab{
    display:inline-flex;
    align-items:center;
    gap:.5rem;
    border-radius:14px;
    padding:.62rem .88rem;
    font-size:12px;
    font-weight:800;
    transition:all .18s ease;
    border:1px solid rgba(255,255,255,.14);
    background:rgba(255,255,255,.10);
    color:#fff;
    backdrop-filter: blur(10px);
  }

  .rep-tab:hover{
    transform: translateY(-1px);
    background: rgba(255,255,255,.14);
  }

  .rep-tab-active{
    background:#ffffff;
    color:#0f172a;
    border-color:transparent;
    box-shadow: 0 10px 24px rgba(15,23,42,.18);
  }

  @media (max-width: 1024px){
    .rep-hero{
      min-height: 126px;
    }

    .rep-hero-grid{
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .rep-hero-title{
      font-size: 24px;
    }
  }

  @media (max-width: 640px){
    .rep-hero{
      min-height: auto;
      border-radius: 22px;
    }

    .rep-hero-title{
      font-size: 21px;
    }

    .rep-hero-text{
      font-size: 12px;
    }

    .rep-search{
      padding: 8px;
    }
  }

    .rep-glass{
      background: rgba(255,255,255,.14);
      border: 1px solid rgba(255,255,255,.22);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      box-shadow: 0 14px 45px rgba(2,6,23,.14);
    }

    .rep-board{
      position: relative;
      margin-top: -34px;
      border-radius: 30px;
      border: 1px solid rgba(226,232,240,.85);
      background: rgba(255,255,255,.80);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      box-shadow: 0 28px 80px rgba(15,23,42,.10);
      overflow: hidden;
    }

    .rep-top-strip{
      background:
        radial-gradient(900px circle at 0% 0%, rgba(99,102,241,.10), transparent 38%),
        radial-gradient(800px circle at 100% 10%, rgba(16,185,129,.08), transparent 34%),
        linear-gradient(180deg, rgba(248,250,252,.9), rgba(255,255,255,.7));
      border-bottom: 1px solid rgba(226,232,240,.85);
    }

    .rep-tab{
      display: inline-flex;
      align-items: center;
      gap: .55rem;
      border-radius: 16px;
      padding: .78rem 1rem;
      font-size: 12.5px;
      font-weight: 800;
      letter-spacing: .01em;
      transition: all .18s ease;
      border: 1px solid rgba(226,232,240,.9);
      background: rgba(255,255,255,.86);
      color: #0f172a;
      box-shadow: 0 8px 24px rgba(15,23,42,.05);
    }

    .rep-tab:hover{
      transform: translateY(-1px);
      background: #fff;
      box-shadow: 0 12px 28px rgba(15,23,42,.08);
    }

    .rep-tab-active{
      color: #fff;
      border-color: rgba(15,23,42,.9);
      background: linear-gradient(135deg, #0f172a, #1e293b);
      box-shadow: 0 14px 36px rgba(15,23,42,.22);
    }

    .rep-search{
      border-radius: 26px;
      border: 1px solid rgba(255,255,255,.24);
      background: rgba(255,255,255,.14);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      box-shadow: 0 14px 45px rgba(2,6,23,.14);
    }

    .rep-search input{
      font-size: 13px !important;
      font-weight: 600;
    }

    .rep-pill{
      display: inline-flex;
      align-items: center;
      gap: .5rem;
      border-radius: 999px;
      padding: .5rem .8rem;
      font-size: 11.5px;
      font-weight: 800;
      letter-spacing: .01em;
    }

    .rep-user-card{
      position: relative;
      overflow: hidden;
      border-radius: 24px;
      border: 1px solid rgba(226,232,240,.9);
      background: linear-gradient(180deg, rgba(255,255,255,.96), rgba(248,250,252,.94));
      box-shadow: 0 14px 45px rgba(15,23,42,.05);
      transition: all .22s ease;
    }

  .rep-user-card:hover{
    transform: translateY(-4px);
    border-color: rgba(203,213,225,.95);
    box-shadow: 0 24px 70px rgba(15,23,42,.10);
  }

  .rep-user-card::after{
    content:"";
    position:absolute;
    inset:0;
    opacity:0;
    transition: opacity .22s ease;
    background:
      radial-gradient(520px circle at 14% 0%, rgba(99,102,241,.12), transparent 38%),
      radial-gradient(580px circle at 100% 18%, rgba(16,185,129,.10), transparent 42%);
    pointer-events:none;
  }

  .rep-user-card:hover::after{
    opacity:1;
  }

  .rep-avatar{
    width: 52px;
    height: 52px;
    border-radius: 18px;
    overflow: hidden;
    box-shadow: 0 12px 28px rgba(15,23,42,.14);
    border: 1px solid rgba(255,255,255,.75);
    background: linear-gradient(135deg, #0f172a, #4f46e5);
  }

  .rep-soft{
    color: #64748b;
  }

  .rep-mini{
    font-size: 11.5px;
  }

  .rep-name{
    font-size: 14px;
    font-weight: 800;
    color: #0f172a;
    letter-spacing: -.01em;
  }

  .rep-btn-view{
    display: inline-flex;
    align-items: center;
    gap: .4rem;
    border-radius: 14px;
    border: 1px solid rgba(226,232,240,.95);
    background: rgba(248,250,252,.95);
    padding: .48rem .72rem;
    font-size: 11.5px;
    font-weight: 800;
    color: #0f172a;
    transition: all .18s ease;
  }

  .rep-user-card:hover .rep-btn-view{
    background: #ffffff;
    border-color: rgba(203,213,225,.95);
  }

  .rep-empty{
    border-radius: 24px;
    border: 1px solid rgba(226,232,240,.9);
    background: linear-gradient(180deg, rgba(255,255,255,.96), rgba(248,250,252,.9));
    box-shadow: 0 14px 45px rgba(15,23,42,.05);
  }

  .rep-pagination a{
    font-size: 12px;
    font-weight: 800;
    border-radius: 16px;
  }

  @media (max-width: 991px){
    .rep-board{
      margin-top: -18px;
    }

    
  }

 
</style>